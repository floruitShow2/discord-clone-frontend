import { cs } from "@/utils/property";

function ContactActivity() {
    return <div className={cs('h-full flex-1 p-3 bg-primary')}>
        <div className={cs('w-full h-full bg-module p-3', 'rounded-xl')}>
        ContactActivity
        </div>
    </div>
}

export default ContactActivity;