import CozeSlice from './coze.slice'
import UserSlice from './user.slice'
import SettingsSlice from './settings.slice'

export default {
  settings: SettingsSlice,
  user: UserSlice,
  coze: CozeSlice
}
